import React from "react";
import Navigation from "./Navigation";

const Header = () => (
  <header>
    <h1>
      Shopping cart demo
    </h1>
    <Navigation />
  </header>
);

export default Header;
